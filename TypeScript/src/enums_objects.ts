//Enum
enum Roles{
    User="USER",
    Admin="ADMIN",
    SuperAdmin="SUPERADMIN"
};

console.log(Roles.Admin);

//Objects
const roles = {
    User: 0,
    Audmin: 1,
    SuperAdmin: 2
};

console.log(roles.User);